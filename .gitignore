# Raw ANVISA source data — only backend/data/MEDICAMENTOS_BUSCA.csv (the slim
# search catalog) is used by the app and committed. The full dumps stay local.
dados remedio/

# E2E / Playwright scratch
.e2e/

# Stray locked worktree dir (Windows handle); not tracked
.claude/worktrees/

# Credenciais de produção — nunca commitar
env-render.txt

# Design sync — artefatos gerados/temporários (durable files em .design-sync/ SÃO commitados)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
